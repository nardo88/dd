name: Deploy Next.js App

on:
  push:
    branches:
      - main # Укажите нужную ветку для деплоя

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Обновленный checkout action

      - name: Setup Node.js
        uses: actions/setup-node@v4 # Обновленный setup-node
        with:
          node-version: 18
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build Next.js app
        run: yarn build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4 # Обновленный upload-artifact
        with:
          name: nextjs-build
          path: |
            .next
            package.json
            yarn.lock
            ecosystem.config.json
          compression-level: 0 # Ускоряет загрузку/выгрузку артефактов

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4 # Обновленный download-artifact
        with:
          name: nextjs-build
          path: nextjs-build

      - name: Deploy to server via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: 'nextjs-build/*'
          target: '/home/nardo/dd'

      - name: Install dependencies and restart application
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/nardo/dd
            yarn install --frozen-lockfile
            pm2 restart ecosystem.config.json || pm2 start ecosystem.config.json
